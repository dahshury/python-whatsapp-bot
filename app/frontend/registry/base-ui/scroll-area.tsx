"use client";

import * as React from "react";
import {
	ScrollArea as InternalScrollArea,
	ScrollBar as InternalScrollBar,
} from "@/components/ui/scroll-area";

export const ScrollArea = InternalScrollArea;
export const ScrollBar = InternalScrollBar;
