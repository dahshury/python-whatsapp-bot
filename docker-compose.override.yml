# Local overrides for development/testing
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    # Remove rclone mount on local machines
    volumes:
      - ./threads_db.sqlite:/app/data/threads_db.sqlite
      - ./.env:/app/.env

  frontend:
    build:
      context: app/frontend
      dockerfile: Dockerfile
    environment:

